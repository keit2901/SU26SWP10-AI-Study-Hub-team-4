# Session Handoff — 2026-07-07

> **RESUME:** đọc file này trước → checkout `feature/admin-upgrade` → fix Npgsql → push PR

## Đã hoàn thành

### Dashboard (Components/Admin/Dashboard.razor)
- KPIs → AdminDashboardStatsDto (real API)
- Activity Trends chart → ActivityTrendsDto (real API)
- Status chart → AdminDashboardStatsDto status counts
- Attention docs → ModerationService.GetQueueAsync (real API)
- Critical Events → AdminApi.ListAuditLogsAsync (real API)
- Daily Token Usage → real data + progress bar + CTA → /admin/users?quota=warning
- Recommended Actions → 4 action items có count + status badge + link
- Retrieval readiness → conditional "No documents yet"
- Notification badges (AdminLayout) → real data

### Users (Components/Admin/Users/Users.razor)
- BuildSeedUsers → AdminApi.ListUsersAsync (real API)
- Tab switcher: All Users | High-usage Users
- Query param ?quota=warning → tự bật High-usage tab
- Empty state "Không có người dùng nào đang vượt ngưỡng"
- TotalUsersCount = _users.Count (was 1284)

### Backend changes
- AdminDashboardStatsDto: thêm IndexedCount, ProcessingCount, PendingCount, DailyTokensUsed, DailyTokenQuota
- DashboardService.GetAdminStatsAsync: query thêm status breakdown + token
- DocumentModerationService: remove Include(Chunks) redundant
- Program.cs: AppDbContext Transient (fix Npgsql circuit sharing)

## Còn tồn tại

### NpgsqlOperationInProgressException
- Dashboard first-load vẫn bị lỗi khi GetQueueAsync chạy cùng lúc với Moderation page
- Root cause: Blazor Server circuit shares DbContext giữa các page
- Đã fix AppDbContext → Transient nhưng chưa hết
- Next fix: bỏ GetQueueAsync khỏi Dashboard, dùng AdminDashboardStatsDto là đủ

## Next session
1. Fix Npgsql: bỏ GetQueueAsync khỏi Dashboard
2. Push PR
3. ADM-06: Escalations (kiểm tra đã real chưa)

## Commands
```powershell
git checkout feature/admin-upgrade
dotnet run --project AI_Study_Hub_v2 --urls http://localhost:5240
```
